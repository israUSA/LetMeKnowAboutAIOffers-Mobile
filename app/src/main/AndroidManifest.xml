<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Obligatorio desde Android 13 para poder mostrar notificaciones. Se pide en contexto,
         la primera vez que el usuario marca una oferta, nunca al arrancar. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Reprograma los recordatorios de reclamo tras un reinicio del dispositivo. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Necesario para abrir el link externo de "Reclamar" en el navegador. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LetMeKnowAboutStudentOffers">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LetMeKnowAboutStudentOffers"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tras un reinicio WorkManager restaura sus work, pero los recordatorios se calculan
             contra `expires_at`: si el dispositivo estuvo días apagado, el plan cambió. El
             receiver recalcula todo con `Notifier.rescheduleAll()`. -->
        <receiver
            android:name=".notifications.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
